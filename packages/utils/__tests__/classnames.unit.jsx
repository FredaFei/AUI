import classes from '../classnames.ts'
describe('classes', () => {
  it('first test', () => {
    const result = classes('button')
    console.log(result)
    // expect(result).toEqual('am-button')
    expect(1).toEqual(1)
  })
})
